services:
  - type: web
    name: marchia-cloud-consultation
    env: python
    buildCommand: |
      echo "🐍 Forçage Python 3.11 depuis runtime.txt"
      cat runtime.txt
      pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 7860
